<view class="register-page" wx:if="{{event}}">
  <!-- 赛事信息 -->
  <view class="event-card">
    <text class="event-name">{{event.title}}</text>
    <text class="event-info">团体赛</text>
  </view>

  <!-- 队伍名称 -->
  <view class="team-card">
    <view class="card-header">
      <text class="card-title">队伍名称</text>
    </view>
    <input
      class="team-name-input"
      placeholder="请输入队伍名称"
      value="{{teamName}}"
      bindinput="onTeamNameInput"
      disabled="{{hasExistingTeam}}"
    />
    <view class="leader-setting">
      <text class="leader-setting-label">领队参赛设置</text>
      <view class="leader-setting-value {{leaderParticipates ? 'on' : 'off'}}">
        {{leaderParticipates ? '领队参赛' : '领队不参赛'}}
      </view>
    </view>
    <text class="leader-setting-tip" wx:if="{{!leaderParticipates}}">
      当前领队不参赛：队伍至少需要3名参赛队员，且不能将领队标记为单打
    </text>
  </view>

  <!-- 队员配置 -->
  <view class="team-card">
    <view class="card-header">
      <text class="card-title">队员配置（{{members.length}} / {{maxMembers}}人）</text>
      <text class="card-subtitle">参赛{{participatingCount}}人，单打最多3人</text>
    </view>
    <view class="team-list">
      <!-- 已有成员 -->
      <view
        class="team-slot confirmed {{item.isSingles ? 'singles-marked' : ''}}"
        wx:for="{{members}}"
        wx:key="user_id"
        bindtap="onToggleSingles"
        data-index="{{index}}"
      >
        <image class="slot-avatar" src="{{item.avatar_url || '/assets/default-avatar.png'}}" mode="aspectFill" />
        <view class="slot-info">
          <text class="slot-name">{{item.name}}{{item.isLeader ? '（领队）' : ''}}</text>
          <text class="slot-position">{{item.status === 'confirmed' ? '已确认' : '待确认'}}</text>
        </view>
        <view class="slot-tags">
          <text class="not-playing-tag" wx:if="{{!item.isParticipating}}">不参赛</text>
          <text class="singles-tag" wx:if="{{item.isSingles}}">单打</text>
        </view>
      </view>

      <!-- 邀请按钮（未满员时显示） -->
      <view class="team-slot empty" wx:if="{{members.length < maxMembers}}">
        <view class="slot-avatar-empty">+</view>
        <view class="slot-info">
          <text class="slot-position-empty">邀请队友</text>
        </view>
        <button class="invite-btn" open-type="share">分享邀请</button>
      </view>
    </view>
  </view>

  <!-- 底部操作 -->
  <view class="bottom-actions">
    <view class="action-row" wx:if="{{!hasExistingTeam}}">
      <button
        class="submit-btn"
        bindtap="onSubmit"
        disabled="{{isSubmitting || participatingCount < minRequiredParticipants}}"
      >
        {{isSubmitting ? '提交中...' : '提交报名'}}
      </button>
      <button
        class="secondary-btn"
        bindtap="onGoBack"
        disabled="{{isSubmitting}}"
      >
        返回
      </button>
    </view>

    <view class="action-row" wx:else>
      <button
        class="submit-btn"
        bindtap="saveSinglesMarks"
        disabled="{{isCanceling}}"
      >
        保存单打标记
      </button>
      <button
        class="danger-btn"
        bindtap="onCancelTeamRegister"
        disabled="{{isCanceling}}"
      >
        {{isCanceling ? '取消中...' : '取消队伍报名'}}
      </button>
    </view>
  </view>
</view>

<view class="loading" wx:if="{{isLoading}}">加载中...</view>
